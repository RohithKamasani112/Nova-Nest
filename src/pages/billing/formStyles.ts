// Shared Tailwind class strings for the 3 billing forms — same look the old
// GenerateBillPage.tsx form used, kept consistent across all 3 new forms.
export const inputClass =
  'w-full min-w-0 rounded-xl border border-gray-200 bg-white px-4 py-3 text-base outline-none transition-colors focus:border-primary focus:ring-2 focus:ring-primary/20 md:text-sm';
export const labelClass = 'mb-1.5 block text-sm font-medium text-text-primary';
export const sectionTitleClass = 'mb-3 text-sm font-bold uppercase tracking-wide text-gray-500';
